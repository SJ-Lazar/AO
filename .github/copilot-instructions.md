# Copilot Instructions

## Project Guidelines
- For AO UI work, prefer a mobile-first design that scales to web, with a desktop-app-like shell where content scrolls within the viewport. Use compact spacing, configurable light/dark themes, and modal/dialog patterns aligned to UI.md.